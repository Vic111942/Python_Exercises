# Write a Python program to compute the distance between the points (x1, y1) and (x2, y2)
p1 = [-3, 5]
p2 = [6, 8]
dx = abs(p1[0] - p2[0])
dy = abs(p1[1] - p2[1])
dis = (dx**2 + dy**2)**0.5
print(dis)
